<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android">
    <font
        android:font="@font/poppins_regular"
        android:fontStyle="normal"
        android:fontWeight="400" /> <!-- Regular weight -->
    <font
        android:font="@font/poppins_medium"
        android:fontStyle="normal"
        android:fontWeight="500" /> <!-- Medium weight -->
    <font
        android:font="@font/poppins_semi_bold"
        android:fontStyle="normal"
        android:fontWeight="600" /> <!-- Semi-Bold weight -->
    <font
        android:font="@font/poppins_bold"
        android:fontStyle="normal"
        android:fontWeight="700" /> <!-- Bold weight -->
    <!-- Add more font tags for other weights as needed -->
</font-family>